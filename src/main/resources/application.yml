spring:
  application:
    name: pixjp-backend

  datasource:
    url: jdbc:postgresql://localhost:5432/pixjp
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: Vyni1995

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
